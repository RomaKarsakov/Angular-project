import { ButtonsHover } from './buttons-hover';

describe('ButtonsHover', () => {
  it('should create an instance', () => {
    const directive = new ButtonsHover();
    expect(directive).toBeTruthy();
  });
});
